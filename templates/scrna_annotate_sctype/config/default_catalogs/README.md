# Built-in ScType Fixtures

Available ids:

```text
builtin:sctype_core
```

This tiny catalog exists for offline smoke tests only. The biological default is `download:sctype`.
Project-specific manual marker annotation belongs in the separate `scrna_annotate_manual_markers`
template.
